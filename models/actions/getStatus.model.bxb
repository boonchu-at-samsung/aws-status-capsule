action (getStatus) {
  type (Search)
  collect {
    input (region) {
      type (region)
      min (Optional) max (One)
    }
    input (service) {
      type (service)
      min (Optional) max (One)
    }
  }
  output (content)
}